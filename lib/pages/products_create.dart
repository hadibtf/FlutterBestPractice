import 'package:flutter/material.dart';

class ProductsCreatePage extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return new Center(
      child: new Text("Create a product"),
    );
  }
}
